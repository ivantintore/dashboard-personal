---
import Layout from '../layouts/Layout.astro';

const privateApps = [
  {
    name: 'AEAT V1 (Simple)',
    description: 'Dashboard básico de notificaciones tributarias',
    icon: '📊',
    url: '/aeat-v1/',
    tech: 'Streamlit',
    color: '#e74c3c',
    active: true
  },
  {
    name: 'AEAT V2 (Avanzado)',
    description: 'Sistema completo con colas asíncronas',
    icon: '⚡',
    url: '/aeat-v2-ui/',
    tech: 'FastAPI + Streamlit + Celery',
    color: '#c0392b',
    active: true
  },
  {
    name: 'Conversor HEIF/PDF',
    description: 'Convierte imágenes HEIC y PDF a JPG',
    icon: '🖼️',
    url: '/conversor/',
    tech: 'FastAPI + Python',
    color: '#9b59b6',
    active: true
  },
  {
    name: 'Intrastat Manager',
    description: 'Procesa archivos Intrastat para AEAT',
    icon: '📦',
    url: '/intrastat/',
    tech: 'JavaScript + Node.js',
    color: '#27ae60',
    active: true
  },
  {
    name: 'Taxi Management',
    description: 'Gestión de flota de taxis Barcelona',
    icon: '🚕',
    url: '/taxi/',
    tech: 'Node.js + SQLite',
    color: '#f39c12',
    active: true
  },
  {
    name: 'Adela Finanzas',
    description: 'Control financiero personal',
    icon: '💰',
    url: '/adela/',
    tech: 'Node.js + SQLite',
    color: '#3498db',
    active: true
  },
  {
    name: 'Toroidal Propellers',
    description: 'Simulador de hélices toroidales',
    icon: '🌀',
    url: '/toroidal/',
    tech: 'Flask + Python',
    color: '#1abc9c',
    active: true
  },
  {
    name: 'Flower Monitor',
    description: 'Monitoreo de tareas Celery',
    icon: '🌸',
    url: '/flower/',
    tech: 'Celery Flower',
    color: '#e91e63',
    active: true
  }
];

const publicApps = [
  {
    name: 'Juegos Clásicos',
    description: 'Colección de juegos retro en JavaScript',
    icon: '🎮',
    url: 'https://github.com/ivantintore/juegos-clasicos',
    tech: 'HTML + JavaScript',
    color: '#2ecc71',
    external: true
  },
  {
    name: 'Pascal a IA Moderna',
    description: 'Curso completo de programación',
    icon: '📚',
    url: 'https://github.com/ivantintore/De-Pascal-a-IA-Moderna-Curso-Completo',
    tech: 'Documentación',
    color: '#3498db',
    external: true
  },
  {
    name: 'SexyFly Reservas',
    description: 'Sistema de reservas para SexyFly',
    icon: '✈️',
    url: 'https://sexyfly-reservas.vercel.app',
    tech: 'Vercel',
    color: '#8e44ad',
    external: true
  }
];

const quickLinks = [
  { icon: '✈️', name: 'SexyFly Reservas', url: 'https://sexyfly-reservas.vercel.app' },
  { icon: '🐙', name: 'GitHub Profile', url: 'https://github.com/ivantintore' },
  { icon: '🌸', name: 'Flower Monitor', url: '/flower/' },
  { icon: '👥', name: 'Gestionar Usuarios', url: '/auth/admin' },
  { icon: '📧', name: 'ivan@maitsa.com', url: 'mailto:ivan@maitsa.com' },
];
---

<Layout title="Dashboard Personal - Ivan Tintore">
  <main>
    <header>
      <div class="header-content">
        <h1>🏠 Dashboard Personal</h1>
        <p class="subtitle">Ivan Tintore • MAITSA</p>
        <div class="header-actions">
          <a href="/auth/admin" class="btn-admin">👥 Usuarios</a>
          <a href="/auth/logout" class="btn-logout">🚪 Salir</a>
        </div>
      </div>
    </header>

    <section class="apps-section">
      <h2>🔒 Aplicaciones Privadas</h2>
      <div class="apps-grid">
        {privateApps.map(app => (
          <a href={app.url} class="app-card" style={`--app-color: ${app.color}`}>
            <div class="app-icon">{app.icon}</div>
            <div class="app-info">
              <h3>{app.name}</h3>
              <p>{app.description}</p>
              <span class="tech-badge">{app.tech}</span>
            </div>
            <div class="status-indicator" class:list={[app.active ? 'active' : 'inactive']}></div>
          </a>
        ))}
      </div>
    </section>

    <section class="apps-section">
      <h2>🌐 Proyectos Públicos</h2>
      <div class="apps-grid">
        {publicApps.map(app => (
          <a href={app.url} target="_blank" rel="noopener" class="app-card external" style={`--app-color: ${app.color}`}>
            <div class="app-icon">{app.icon}</div>
            <div class="app-info">
              <h3>{app.name} <span class="external-icon">↗</span></h3>
              <p>{app.description}</p>
              <span class="tech-badge">{app.tech}</span>
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- Quick Links -->
    <section class="quick-section">
      <h2>⚡ Accesos Rápidos</h2>
      <div class="quick-links">
        {quickLinks.map(link => (
          <a href={link.url} target={link.url.startsWith('http') ? '_blank' : '_self'} rel="noopener" class="quick-link">
            <span class="quick-icon">{link.icon}</span>
            <span class="quick-name">{link.name}</span>
          </a>
        ))}
      </div>
    </section>

    <footer>
      <p>© 2024 Ivan Tintore • Dashboard v2.0 • MAITSA</p>
    </footer>
  </main>
</Layout>

<style>
  :root {
    --bg-dark: #0f0f1a;
    --bg-card: #1a1a2e;
    --text-primary: #ffffff;
    --text-secondary: #a0a0b0;
    --accent: #6366f1;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--bg-dark);
    color: var(--text-primary);
    min-height: 100vh;
  }

  main {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem;
  }

  header {
    margin-bottom: 3rem;
    text-align: center;
  }

  .header-content h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
    background: linear-gradient(135deg, #6366f1, #a855f7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    color: var(--text-secondary);
    font-size: 1.1rem;
  }

  .header-actions {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .btn-admin, .btn-logout {
    padding: 0.6rem 1.2rem;
    border-radius: 8px;
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.2s;
  }

  .btn-admin {
    background: #374151;
    color: white;
  }

  .btn-admin:hover {
    background: #4b5563;
  }

  .btn-logout {
    background: #dc2626;
    color: white;
  }

  .btn-logout:hover {
    background: #ef4444;
  }

  .apps-section, .quick-section {
    margin-bottom: 3rem;
  }

  .apps-section h2, .quick-section h2 {
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
    color: var(--text-secondary);
  }

  .apps-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  .app-card {
    background: var(--bg-card);
    border-radius: 16px;
    padding: 1.5rem;
    text-decoration: none;
    color: inherit;
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    transition: all 0.3s ease;
    border: 1px solid transparent;
    position: relative;
    overflow: hidden;
  }

  .app-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: var(--app-color);
  }

  .app-card:hover {
    transform: translateY(-4px);
    border-color: var(--app-color);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
  }

  .app-icon {
    font-size: 2.5rem;
    flex-shrink: 0;
  }

  .app-info {
    flex: 1;
  }

  .app-info h3 {
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .app-info p {
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin-bottom: 0.75rem;
    line-height: 1.4;
  }

  .tech-badge {
    display: inline-block;
    background: rgba(99, 102, 241, 0.2);
    color: #818cf8;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 500;
  }

  .status-indicator {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    position: absolute;
    top: 1rem;
    right: 1rem;
  }

  .status-indicator.active {
    background: #22c55e;
    box-shadow: 0 0 10px #22c55e;
  }

  .status-indicator.inactive {
    background: #ef4444;
  }

  .external-icon {
    font-size: 0.8rem;
    opacity: 0.6;
  }

  .app-card.external {
    opacity: 0.85;
  }

  .app-card.external:hover {
    opacity: 1;
  }

  /* Quick Links */
  .quick-links {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }

  .quick-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    background: var(--bg-card);
    border-radius: 12px;
    text-decoration: none;
    color: var(--text-primary);
    transition: all 0.2s ease;
    border: 1px solid transparent;
  }

  .quick-link:hover {
    background: #252540;
    border-color: var(--accent);
    transform: translateY(-2px);
  }

  .quick-icon {
    font-size: 1.2rem;
  }

  .quick-name {
    font-size: 0.9rem;
    font-weight: 500;
  }

  footer {
    text-align: center;
    padding: 2rem 0;
    color: var(--text-secondary);
    font-size: 0.9rem;
    border-top: 1px solid rgba(255,255,255,0.1);
    margin-top: 2rem;
  }

  @media (max-width: 768px) {
    main {
      padding: 1rem;
    }

    .header-content h1 {
      font-size: 1.8rem;
    }

    .apps-grid {
      grid-template-columns: 1fr;
    }

    .header-actions {
      flex-direction: column;
      align-items: center;
    }

    .quick-links {
      flex-direction: column;
    }

    .quick-link {
      width: 100%;
      justify-content: center;
    }
  }
</style>
